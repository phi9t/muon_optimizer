#+title: Qwen3 Layer-Streaming Workstream
#+startup: overview

* Task Description
Build a constrained-memory Qwen3 execution path for the existing logits explorer workflow.

The current script can compare Qwen3 student and teacher logits by loading each full model on CPU. On this 8 GiB MacBook Air, that full-load approach risks excessive memory use. The new path should keep peak RSS under a 6 GiB cap by streaming model execution layer by layer from Hugging Face safetensors and dropping weights as soon as each stage completes.

The student model is =Qwen/Qwen3-0.6B=. The teacher model is =Qwen/Qwen3-1.7B=. The student should generate tokens greedily, and the teacher should process the same generated prefixes to produce comparable logits. Both models need per-layer KV cache, stored locally on disk and loaded only as each layer needs it.

Initial scope may be Qwen3-family-specific. It does not need to be a general Hugging Face execution engine.

* Implementation Prompt
Implement the design in =.workstreams/qwen-layer-streaming/design.md=.

Add a constrained runner path that:

- uses CPU only,
- defaults to a 6 GiB RSS memory cap,
- streams Qwen3 weights one stage at a time from safetensors,
- spills per-layer KV cache to local disk,
- lets the 0.6B student greedily generate a short continuation,
- runs the 1.7B teacher on the same generated token positions,
- compares next-token logits at each generated step,
- writes explorer-consumable JSON with prompt-level and step-level metrics,
- keeps the existing full-model comparison path available.

Prefer using Hugging Face Qwen3 module classes one layer at a time. If that cannot stay under the memory cap, implement only the Qwen3 operations needed for these checkpoints.

* Implementation Plan
** Task 1: Package Skeleton And CLI Shape
*** Files
- Create: =scripts/qwen_streaming/__init__.py=
- Create: =scripts/qwen_streaming/spec.py=
- Create: =scripts/qwen_streaming/weights.py=
- Create: =scripts/qwen_streaming/memory.py=
- Create: =scripts/qwen_streaming/kv_cache.py=
- Create: =scripts/qwen_streaming/model_ops.py=
- Create: =scripts/qwen_streaming/streamer.py=
- Create: =scripts/qwen_streaming/comparison.py=
- Modify: =scripts/run_qwen_logits_comparison.py=

*** Steps
- [X] Add the package skeleton with importable modules and no side-effect imports of =torch= at package import time.
- [X] Extend the existing CLI with =--mode full|streamed=, =--memory-cap-gb 6.0=, =--max-new-tokens 8=, =--kv-cache-dir .qwen_kv_cache=, and =--dry-plan=.
- [X] Preserve current behavior when =--mode full= or no mode is supplied.
- [X] Route =--mode streamed= to a temporary guarded entrypoint that raises =NotImplementedError("streamed Qwen comparison is not implemented yet")=; Task 6 replaces this with the real comparison loop.
- [X] Run =python3 scripts/run_qwen_logits_comparison.py --help= and confirm the new flags appear.
- [X] Run =python3 -m py_compile scripts/run_qwen_logits_comparison.py scripts/qwen_streaming/*.py=.
- [X] Commit with message =Add streamed Qwen CLI skeleton=.

** Task 2: Metadata And Weight Planning
*** Files
- Modify: =scripts/qwen_streaming/spec.py=
- Modify: =scripts/qwen_streaming/weights.py=
- Create: =qwen_streaming_spec_test.py=
- Modify: =pyproject.toml= if a dependency is needed.
- Modify: =uv.lock= if dependencies change.

*** Steps
- [X] Implement =QwenStreamedModelSpec.from_model_id(model_id: str)= using =huggingface_hub.snapshot_download= with metadata and safetensor allow patterns.
- [X] Implement config validation for =model_type == "qwen3"= or compatible Qwen3 causal LM metadata, positive layer counts, positive vocab size, and matching attention-head divisibility.
- [X] Implement required tensor planning for =model.embed_tokens.weight=, =model.layers.{i}.*=, =model.norm.weight=, and =lm_head.weight= when embeddings are not tied.
- [X] Implement index parsing for =model.safetensors.index.json=.
- [X] Implement single-file safetensors planning by inspecting =model.safetensors= keys with =safetensors.safe_open=.
- [X] Implement =SafetensorWeightLoader.load_tensor= and =load_tensors= using =safe_open= and CPU =torch.float32= output.
- [X] Add tests with tiny temporary safetensors files covering indexed checkpoints, single-file checkpoints, and missing-key errors.
- [X] Run =uv run --group qwen-logits pytest qwen_streaming_spec_test.py -v=.
- [X] Commit with message =Add Qwen streaming checkpoint planning=.

** Task 3: Memory Guard And KV Spill Store
*** Files
- Modify: =scripts/qwen_streaming/memory.py=
- Modify: =scripts/qwen_streaming/kv_cache.py=
- Create: =qwen_streaming_memory_test.py=
- Create: =qwen_streaming_kv_cache_test.py=
- Modify: =pyproject.toml=
- Modify: =uv.lock=

*** Steps
- [X] Add =psutil>=5.9= to the =qwen-logits= dependency group and update =uv.lock=.
- [X] Implement =MemoryGuard(cap_gb: float)= with =check(label: str) -> int= returning current RSS bytes and raising =MemoryError= when over cap.
- [X] Include the stage label, RSS in GiB, and cap in GiB in the =MemoryError= message.
- [X] Implement =LayerKV= with =key= and =value= tensors.
- [X] Implement =KVSpillStore.read(layer_index)= returning =None= when no cache exists.
- [X] Implement =KVSpillStore.write(layer_index, kv)= with atomic same-directory replace.
- [X] Implement =KVSpillStore.clear()= to remove the prompt cache directory.
- [X] Test low-cap failure, generous-cap success, KV round trip, overwrite, and clear behavior.
- [X] Run =uv run --group qwen-logits pytest qwen_streaming_memory_test.py qwen_streaming_kv_cache_test.py -v=.
- [X] Commit with message =Add memory guard and KV spill store=.

** Task 4: Qwen3 Manual Tensor Ops
*** Files
- Modify: =scripts/qwen_streaming/model_ops.py=
- Create: =qwen_streaming_model_ops_test.py=

*** Steps
- [X] Implement =rms_norm(hidden, weight, eps)= using the Qwen RMSNorm formula =hidden * rsqrt(mean(hidden^2) + eps) * weight=.
- [X] Implement rotary embedding helpers for Qwen3 attention using =rope_theta= and explicit integer positions.
- [X] Implement grouped-query key/value repetition for =num_attention_heads / num_key_value_heads=.
- [X] Implement causal scaled dot-product attention for CPU tensors.
- [X] Implement Qwen gated MLP: =down_proj(silu(gate_proj(hidden)) * up_proj(hidden))=.
- [X] Test each helper with tiny deterministic tensors and direct PyTorch reference formulas.
- [X] Run =uv run --group qwen-logits pytest qwen_streaming_model_ops_test.py -v=.
- [X] Commit with message =Add Qwen3 streaming tensor ops=.

** Task 5: Layer Streamer
*** Files
- Modify: =scripts/qwen_streaming/streamer.py=
- Create: =qwen_streaming_streamer_test.py=

*** Steps
- [X] Implement =QwenLayerStreamer.prefill(input_ids, cache)=.
- [X] During prefill, load token embeddings, produce hidden states, free embedding tensors, then process layers from =0= to =num_hidden_layers - 1=.
- [X] For each layer, load only that layer's norm, attention, and MLP tensors; compute output hidden states; write that layer's KV cache; delete loaded tensors; call =gc.collect()=; and run =MemoryGuard.check=.
- [X] Implement =QwenLayerStreamer.decode_one(token_id, position, cache)=.
- [X] During decode, load the existing layer KV from disk, append the new key/value for the generated token, write updated KV back, and continue to the next layer.
- [X] Implement =logits_from_hidden(hidden)= using final norm and LM head loaded only for that call.
- [X] Test shape correctness with a tiny synthetic Qwen-like checkpoint and one or two layers.
- [X] Run =uv run --group qwen-logits pytest qwen_streaming_streamer_test.py -v=.
- [X] Commit with message =Add Qwen layer streamer=.

** Task 6: Streamed Generation Comparison
*** Files
- Modify: =scripts/qwen_streaming/comparison.py=
- Modify: =scripts/run_qwen_logits_comparison.py=
- Create: =qwen_streaming_output_test.py=
- Modify: =explorer/src/qwen/types.ts= if step-aware schema changes are needed.
- Modify: =explorer/src/qwen/QwenLogitsExplorer.tsx= if step selector support is needed.

*** Steps
- [X] Implement streamed comparison orchestration that pre-fills student and teacher for one prompt at a time.
- [X] Generate tokens greedily from the student using =torch.argmax(student_logits)=.
- [X] Decode the teacher for the same generated token and position.
- [X] Reuse existing metric logic for teacher/student logits at each generated step.
- [X] Emit =metadata=, =prompts[].generated_text=, =prompts[].steps[]=, prompt-level aggregate metrics, and global =aggregate=.
- [X] Keep fixed-prompt final-logit output readable by the explorer, or update the explorer to handle both the original and step-aware shape.
- [X] Test JSON shape with fake logits and a fake tokenizer.
- [X] Run =uv run --group qwen-logits pytest qwen_streaming_output_test.py -v=.
- [X] Run =npm --prefix explorer run build= if explorer files changed.
- [X] Commit with message =Add streamed Qwen generation comparison=.

** Task 7: Dry Plan And Runtime Smoke
*** Files
- Modify: =scripts/run_qwen_logits_comparison.py=
- Modify: =scripts/qwen_streaming/comparison.py=
- Modify: =scripts/qwen_streaming/spec.py=

*** Steps
- [X] Implement =--dry-plan= for streamed mode so it resolves model metadata, validates tokenizer compatibility, validates required tensor keys, prints layer/vocab/cache/memory-cap details, and exits before model math.
- [X] Run =uv run --group qwen-logits python scripts/run_qwen_logits_comparison.py --mode streamed --dry-plan --student-model Qwen/Qwen3-0.6B --teacher-model Qwen/Qwen3-1.7B=.
- [X] If dry-plan succeeds, run =uv run --group qwen-logits python scripts/run_qwen_logits_comparison.py --mode streamed --limit-prompts 1 --max-new-tokens 1 --memory-cap-gb 6=.
- [X] If the smoke run is too slow or blocked by model download, capture the exact reason in the final handoff and leave generated JSON uncommitted.
- [X] Run =python3 -m py_compile scripts/run_qwen_logits_comparison.py scripts/qwen_streaming/*.py=.
- [X] Run the focused Python tests added by this workstream.
- [X] Run =npm --prefix explorer run build=.
- [X] Commit with message =Verify streamed Qwen runner= if any verification-only code or docs changed.

* Acceptance Criteria
** Required
- [X] The constrained runner is CPU-only.
- [X] Default memory cap is 6 GiB RSS and is configurable.
- [X] The runner checks RSS at meaningful stage boundaries and fails clearly if the cap is exceeded.
- [X] Student and teacher tokenizer ID mappings are validated before logits are compared.
- [X] Student and teacher vocab sizes are validated before logits are compared.
- [X] Required Qwen3 safetensors keys are validated before long execution begins.
- [X] At most one model stage's weights are intentionally resident at a time.
- [X] KV cache is spilled per model and per layer to local disk.
- [X] Student generation is deterministic by default, using greedy decoding.
- [X] Teacher logits are computed for the same generated token positions as the student.
- [X] Output JSON includes metadata, prompt-level summaries, generation steps, top-k token payloads, overlap, KL divergence, cosine similarity, mean absolute logit delta, and max absolute logit delta.
- [X] Existing explorer UI can show missing-data empty state without breaking.
- [X] Existing full-model comparison path remains available unless deliberately replaced by an equivalent CLI mode.

** Verification
- [X] =python3 scripts/run_qwen_logits_comparison.py --help= succeeds or the new constrained command's =--help= succeeds.
- [X] A dry-plan or metadata validation mode succeeds without loading full model weights.
- [X] Unit tests cover safetensors index parsing, tensor key planning, tokenizer compatibility, memory guard behavior, KV spill round trip, and JSON shape.
- [X] =npm --prefix explorer run build= succeeds after any explorer schema/UI changes.
- [X] A minimal runtime smoke test with one prompt and one generated token is attempted on this machine, unless explicitly skipped because model download/runtime is too expensive.

** Non-Goals
- [X] Do not build a fully general transformer execution engine.
- [X] Do not optimize for speed before memory containment.
- [X] Do not require GPU, MPS, or CUDA.
- [X] Do not commit downloaded model weights or generated Hugging Face cache files.

* Notes
- Physical memory on this machine is 8 GiB.
- The cap was chosen as 6 GiB to leave headroom for macOS and shell/browser activity.
- KV cache stores attention keys and values, not hidden states.
- Full constrained execution can be slow; implementation should support small smoke runs first.
- Implementation completed on 2026-05-26 using codex-spark worker agents for Tasks 1-7, followed by local compatibility fixes for real Qwen3 vocab/head-shape behavior.
- Dry-plan verification succeeded for =Qwen/Qwen3-0.6B= student and =Qwen/Qwen3-1.7B= teacher.
- Minimal runtime smoke succeeded with =--mode streamed --limit-prompts 1 --max-new-tokens 1 --memory-cap-gb 6 --output /tmp/qwen_stream_smoke.json=.
- Smoke output contained one prompt, one generated step, and generated token id =576= with text =" The"=.
